# Backend (NestJS)

Auth + users API for KPD.

## Scripts

```bash
npm run start:dev   # watch mode
npm run build
npm run start:prod
```

## Env

Copy `.env.example` → `.env`. See root `docker-compose.yml` for Postgres.
